{% extends "::base.html.twig" %}

{% block title %}Lista Wydatków{% endblock %}

{% block body %}
<h2>Twoje wydatki</h2>

<ul>
{% for expend in expends %}
    <li>{{expend.description}} {{expend.amount}} {{ expend.expendDate|date("d/m/Y")}}
    {{expend.account.name}}</li>
    {% endfor %}
</ul>
    
    <br>
    <a href="{{ path('new_expend') }}">Dodaj nowy wydatek</a>
    <br>
    <br>
    
    <a href="{{ path('Panel') }}">Strona główna</a>
    <br>
    <br>
    <a href="{{ path('fos_user_security_logout') }}">Wyloguj</a>

{% endblock %}
