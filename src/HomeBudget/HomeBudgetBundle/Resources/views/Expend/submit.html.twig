{% extends "::base.html.twig" %}

{% block title %}Potwierdzenie{% endblock %}

{% block body %}
    <h2>Potwierdź usunięcie poniższego wydatku</h2>

    <p>Opis: {{expend.description}}</p> 
    <p>Kategoria: {{expend.expendCategory.name}}</p>
    <p>Kwota: {{expend.amount}}</p>
    <p>Data: {{expend.expendDate|date("d/m/Y")}}</p> 
    <p>Z konta: {{expend.account.name}}</p>

    <a href="{{ path('delete_expend', {"id" : expend.id}) }}"><button>Potwierdź</button></a>
    <br>
    <br>
    <a href="{{ path('new_expend') }}">Dodaj nowy wydatek</a>
    <br>
    <br>
    <a href="{{ path('show_allExpends') }}">Wydatki</a>
    <br>
    <br>
    <a href="{{ path('show_allIncomes') }}">Przychody</a>
    <br>
    <br>
    <a href="{{ path('show_allAccounts') }}">Konta</a>
    <br>
    <br>
    <a href="{{ path('Panel') }}">Strona główna</a>
    <br>
    <br>
    <a href="{{ path('fos_user_security_logout') }}">Wyloguj</a>   

{% endblock %}

