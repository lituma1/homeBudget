{% extends "::base.html.twig" %}

{% block title %}Potwierdzenie{% endblock %}

{% block body %}
    <h1>{{message}}</h1>
    <h2>Potwierdź usunięcie poniższego przychodu</h2>

    <p>Opis: {{income.description}}</p> 
    <p>Kategoria: {{income.incomeCategory.name}}</p>
    <p>Kwota: {{income.amount}}</p>
    <p>Data: {{income.incomeDate|date("d/m/Y")}}</p> 
    <p>Na konto: {{income.account.name}}</p>

    <a href="{{ path('delete_income', {"id" : income.id}) }}"><button>Potwierdź</button></a>
    <br>
    <br>
    <a href="{{ path('new_income') }}">Dodaj nowy przychód</a>
    <br>
    <br>
    <a href="{{ path('show_allExpends') }}">Wydatki</a>
    <br>
    <br>
    <a href="{{ path('show_allIncomes') }}">Przychody</a>
    <br>
    <br>
    <a href="{{ path('show_allAccounts') }}">Konta</a>
    <br>
    <br>
    <a href="{{ path('Panel') }}">Strona główna</a>
    <br>
    <br>
    <a href="{{ path('fos_user_security_logout') }}">Wyloguj</a>   

{% endblock %}

