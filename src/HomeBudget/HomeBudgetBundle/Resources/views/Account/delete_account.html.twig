{% extends "::base.html.twig" %}

{% block title %}Potwierdzenie{% endblock %}

{% block body %}
    <h2>Potwierdź usunięcie poniższego konta</h2>

    <p>Nazwa: {{account.name}}</p> 
    <p>Saldo: {{account.balance}}</p>
    <p>Typ: {{account.type.name}}</p>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    <br>
    <a href="{{ path('new_Account') }}">Dodaj nowe konto</a>
    <br>
    <br>
    <a href="{{ path('show_allExpends') }}">Wydatki</a>
    <br>
    <br>
    <a href="{{ path('show_allIncomes') }}">Przychody</a>
    <br>
    <br>
    <a href="{{ path('Panel') }}">Strona główna</a>
    <br>
    <br>
    <a href="{{ path('fos_user_security_logout') }}">Wyloguj</a>   

{% endblock %}

